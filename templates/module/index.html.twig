{% extends 'base.html.twig' %}

{% block contenu %}
    <h2>Liste des modules</h2>
    <table class="uk-table uk-table-striped uk-table-responsive">
        <thead>
            <tr>
               <th>Libellé</th>
               <th>Catégorie</th>
               <th>Programme<th>
            </tr>
        </thead>
            {% for module in modules %}
               <tr> 
                  <td>{{ module.libelle }}</td>
                  <td>{{ module.categorie }}</td>
                  <td>{{ module.programmers }}</td>
                </tr>
            {% endfor %}
        <tbody>
    </table>
{% endblock %}
